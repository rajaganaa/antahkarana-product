# Antahkarana MedAssist — Backend Dependencies
# CPU-only Azure deployment. NO vLLM. NO torch. NO accelerate.
# LLM: Groq API (llama3-70b-8192)
# Vision: GitHub Models (GPT-4o)
# Author: RAJAGANAPATHY M, SRM University

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# ── LLM: Groq (PRIMARY — replaces vLLM) ──────────────────────────────────────
groq>=0.9.0

# ── LLM: GitHub Models / OpenAI-compatible (fallback) ────────────────────────
openai>=1.25.0

# ── Embeddings (CPU, no GPU required) ────────────────────────────────────────
sentence-transformers>=2.6.0

# ── Image processing (for base64 encoding in vision module) ──────────────────
Pillow>=10.0.0

# ── RAG + Vector DB ───────────────────────────────────────────────────────────
chromadb>=0.5.0
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
pypdf>=4.0.0

# ── MedAssist Tools ───────────────────────────────────────────────────────────
requests>=2.31.0

# ── Utilities ─────────────────────────────────────────────────────────────────
numpy>=1.24.0
python-dotenv>=1.0.0
pydantic>=2.0.0
